# Bounded Contexts 정의

> 📋 **문서 목적**: Mermaid Saiko 프로젝트의 Bounded Context 정의 및 책임 분리

**마지막 업데이트**: 2025-10-26

---

## 개요

Mermaid Saiko는 **3개의 Bounded Context**로 구성됩니다.

### Context 분리 이유

- **Editor Context**: 웹 UI에서만 필요 (MCP는 불필요)
- **Rendering Context**: 핵심 도메인 로직 (모든 인터페이스에서 사용)
- **Export Context**: 이미지 생성 및 변환 (API, MCP에서 사용)

### Context Map

```
┌─────────────────┐
│ Editor Context  │ (웹 전용)
│                 │
│ - 코드 입력     │
│ - 실시간 반영   │
└────────┬────────┘
         │ uses
         ↓
┌─────────────────┐
│Rendering Context│ (핵심 도메인)
│                 │
│ - 다이어그램 변환│
│ - 에러 처리     │
│ - 타입 지원     │
└────────┬────────┘
         │ uses
         ↓
┌─────────────────┐
│ Export Context  │ (변환)
│                 │
│ - PNG 생성      │
│ - SVG 생성      │
│ - 크기 조정     │
└─────────────────┘
```

---

## 1️⃣ Editor Context

### 책임

웹 브라우저에서 사용자가 Mermaid 코드를 편집하고 실시간으로 확인하는 UI 관련 로직을 담당합니다.

### 주요 기능

| 기능 ID | 기능명 | 설명 |
|---------|--------|------|
| EDITOR-01 | 코드 입력 | 웹 에디터에서 Mermaid 코드 입력 |
| EDITOR-02 | 실시간 반영 | 코드 수정 시 즉시 다이어그램 반영 (디바운싱) |
| EDITOR-03 | 기본 편집 기능 | 복사, 붙여넣기, 되돌리기 |

### 핵심 개념

- **Code Editor**: 사용자가 입력한 Mermaid 코드를 관리
- **Live Preview**: 실시간으로 렌더링 결과 표시
- **Debouncing**: 과도한 렌더링 요청 방지

### 의존성

- **Rendering Context** 사용: 코드를 다이어그램으로 변환 요청

### 특징

- **웹 인터페이스 전용**: API, MCP 서버에서는 사용하지 않음
- **상태 관리**: 프론트엔드 상태 관리 (코드, 커서 위치 등)

---

## 2️⃣ Rendering Context

### 책임

Mermaid 코드를 다이어그램으로 변환하는 **핵심 도메인 로직**을 담당합니다.

### 주요 기능

| 기능 ID | 기능명 | 설명 |
|---------|--------|------|
| RENDER-01 | 다이어그램 변환 | Mermaid 코드를 다이어그램으로 변환 |
| RENDER-02 | 실시간 프리뷰 | 코드 수정 시 자동 업데이트 |
| RENDER-03 | 모든 타입 지원 | 모든 Mermaid 다이어그램 타입 지원 |
| RENDER-04 | 에러 표시 | 잘못된 코드에 대한 에러 메시지 |
| RENDER-05 | API 변환 | API를 통한 변환 요청 처리 |

### 핵심 개념

- **Diagram**: 렌더링된 다이어그램 (SVG DOM 형태)
- **MermaidCode**: 입력된 Mermaid 문법 코드
- **DiagramType**: flowchart, sequence, class, ER, gantt 등
- **RenderingError**: 파싱 또는 렌더링 실패 시 에러 정보

### 의존성

- **독립적**: 다른 Context에 의존하지 않음 (순수 도메인 로직)
- **Mermaid.js 라이브러리** 사용

### 특징

- **모든 인터페이스에서 사용**: 웹, API, MCP
- **순수 비즈니스 로직**: 프레임워크 독립적

---

## 3️⃣ Export Context

### 책임

렌더링된 다이어그램을 다양한 포맷의 이미지 파일로 변환하고 내보내는 로직을 담당합니다.

### 주요 기능

| 기능 ID | 기능명 | 설명 |
|---------|--------|------|
| EXPORT-01 | PNG 다운로드 | PNG 파일로 다운로드 |
| EXPORT-02 | SVG 다운로드 | SVG 파일로 다운로드 |
| EXPORT-03 | API 이미지 생성 | API를 통한 이미지 변환 |
| EXPORT-04 | 파일명 지정 | 다운로드 파일명 지정 |
| EXPORT-05 | 크기 조정 | 이미지 크기 조정 |

### 핵심 개념

- **Image**: 변환된 이미지 (PNG, SVG)
- **ImageFormat**: 이미지 포맷 (png, svg)
- **ImageOptions**: 파일명, 크기 등 옵션
- **ExportResult**: 변환된 이미지 데이터 및 메타데이터

### 의존성

- **Rendering Context** 사용: 렌더링된 다이어그램을 이미지로 변환

### 특징

- **API, MCP에서 주로 사용**: 웹에서도 다운로드 시 사용
- **다양한 포맷 지원**: PNG, SVG (향후 PDF 등 확장 가능)

---

## Context 간 관계

### 의존성 방향

```
Editor Context → Rendering Context → Export Context
```

### 호출 플로우

**웹 인터페이스**:
```
1. Editor Context: 사용자가 코드 입력
2. Rendering Context: 코드를 다이어그램으로 변환
3. (선택) Export Context: 사용자가 다운로드 버튼 클릭 시 이미지 생성
```

**API/MCP 인터페이스**:
```
1. Rendering Context: API 요청으로 코드를 다이어그램으로 변환
2. Export Context: 다이어그램을 이미지로 변환하여 반환
```

### 통신 방식

- **동기 호출**: 모든 Context 간 통신은 동기식 (이벤트 불필요)
- **직접 의존**: Editor → Rendering → Export

---

## 승인 상태

**전체 승인 상태**: ✅ (승인 완료)

**승인 체크리스트**:
- [x] Context 분리 전략 승인 (3개 Context)
- [x] 각 Context의 책임 범위 승인
- [x] Context 간 의존성 방향 승인

**승인 날짜**: 2025-10-26

---

> 📎 **관련 문서**:
> - `documents/requirements/render-requirements.md`
> - `documents/requirements/export-requirements.md`
> - `documents/requirements/editor-requirements.md`
