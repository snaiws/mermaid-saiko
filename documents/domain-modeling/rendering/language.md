# Rendering Context - Ubiquitous Language

> 📋 **문서 목적**: Rendering Context의 공통 언어 사전

**마지막 업데이트**: 2025-10-26

---

## 핵심 용어

### Diagram (다이어그램)
Mermaid 코드를 시각적으로 렌더링한 결과물. SVG 형식으로 표현됩니다.

**영역**: Aggregate Root

**예시**: flowchart, sequence diagram, class diagram

---

### Mermaid Code (머메이드 코드)
Mermaid 문법으로 작성된 텍스트 기반 다이어그램 정의. 사용자가 입력하는 원본 코드입니다.

**영역**: Value Object

**예시**:
```
graph LR
    A[시작] --> B[처리]
    B --> C[종료]
```

---

### Diagram Type (다이어그램 타입)
Mermaid가 지원하는 다이어그램의 종류.

**영역**: Enum

**값**: flowchart, sequence, class, state, er, gantt, pie, git, journey, mindmap, timeline

---

### Rendering (렌더링)
Mermaid 코드를 파싱하여 SVG 형식의 다이어그램으로 변환하는 과정.

**동사형**: render

**결과**: SVG 문자열

---

### Render Status (렌더링 상태)
다이어그램 렌더링 작업의 현재 상태.

**영역**: Enum

**값**:
- `pending`: 렌더링 대기 중
- `success`: 렌더링 성공
- `failed`: 렌더링 실패

---

### Rendered SVG (렌더링된 SVG)
렌더링이 성공적으로 완료된 후 생성된 SVG 문자열.

**영역**: Diagram의 속성

**형식**: string (SVG XML)

---

### Rendering Error (렌더링 에러)
Mermaid 코드 파싱 또는 렌더링 중 발생한 오류 정보.

**영역**: Value Object

**포함 정보**: 에러 메시지, 발생 라인, 발생 컬럼

---

### Parsing (파싱)
Mermaid 코드를 문법적으로 분석하는 과정. 렌더링의 첫 단계입니다.

**실패 시**: Rendering Error 발생

---

## 행위 용어

### Create Diagram (다이어그램 생성)
새로운 Diagram을 생성하고 즉시 렌더링을 시작하는 행위.

**입력**: Mermaid Code

**출력**: Diagram (Aggregate)

---

### Render (렌더링하다)
Mermaid 코드를 SVG로 변환하는 행위.

**성공 시**: `DiagramRendered` 이벤트 발행

**실패 시**: `DiagramRenderFailed` 이벤트 발행

---

### Update Code (코드 업데이트)
기존 Diagram의 Mermaid 코드를 새로운 코드로 변경하고 재렌더링하는 행위.

**트리거**: 사용자의 코드 수정, Undo/Redo

**결과**: `DiagramCodeUpdated` 이벤트 발행

---

### Get SVG (SVG 가져오기)
렌더링이 성공한 Diagram에서 SVG 문자열을 반환하는 행위.

**전제 조건**: `renderStatus === SUCCESS`

**예외**: 렌더링 실패 상태에서 호출 시 예외 발생

---

### Get Error (에러 가져오기)
렌더링이 실패한 Diagram에서 에러 정보를 반환하는 행위.

**전제 조건**: `renderStatus === FAILED`

**예외**: 성공 상태에서 호출 시 예외 발생

---

## 비즈니스 규칙 용어

### Valid Mermaid Code (유효한 머메이드 코드)
Mermaid 문법에 맞고, 빈 문자열이 아닌 코드.

**검증**: 코드가 비어있지 않고, 파싱이 성공해야 함

---

### Successful Rendering (성공적인 렌더링)
Mermaid 코드가 에러 없이 SVG로 변환된 상태.

**조건**: `renderStatus === SUCCESS` AND `renderedSvg !== null` AND `error === null`

---

### Failed Rendering (실패한 렌더링)
Mermaid 코드 파싱 또는 렌더링 중 에러가 발생한 상태.

**조건**: `renderStatus === FAILED` AND `error !== null` AND `renderedSvg === null`

---

## 승인 상태

**전체 승인 상태**: ✅ (승인 완료)

**승인 체크리스트**:
- [x] 핵심 용어 정의 승인
- [x] 행위 용어 정의 승인
- [x] 비즈니스 규칙 용어 승인

**승인 날짜**: 2025-10-26

---

> 📎 **관련 문서**:
> - `documents/domain-modeling/bounded-contexts.md`
> - `documents/domain-modeling/rendering/aggregates.md`
> - `documents/domain-modeling/rendering/events.md`
