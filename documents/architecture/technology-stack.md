# 기술 스택 (Technology Stack)

> 📋 **문서 목적**: Mermaid Saiko 프로젝트의 기술 스택 선정 및 이유

**마지막 업데이트**: 2025-10-26

---

## 1️⃣ 언어 및 핵심 프레임워크 선정

### 백엔드

#### 언어: TypeScript

**선택**: TypeScript

**버전**: `^5.3.0` (최신 안정 버전)

**선정 이유**:
- 타입 안전성으로 런타임 에러 감소
- IDE 자동완성 및 리팩토링 지원
- JavaScript 생태계와 완벽한 호환
- DDD 구현 시 명확한 타입 정의 가능
- NestJS와 완벽한 통합

**대안 검토**:
- **Go**: 성능 우수, 컴파일 언어
  - 장점: 빠른 속도, 간결한 문법
  - 단점: 생태계 작음, Mermaid.js 사용 불가 (Node.js 전용)
- **Java/Kotlin**: 엔터프라이즈 표준
  - 장점: 안정성, 타입 안전성
  - 단점: 보일러플레이트 많음, 개발 속도 느림

---

#### 백엔드 프레임워크: NestJS

**선택**: NestJS

**버전**: `^10.x` (최신 메이저 버전)

**기본 플랫폼**: Express (기본값, 향후 Fast ify 고려)

**선정 이유**:
- TypeScript 네이티브 지원
- DDD 친화적 모듈 구조 (Module, Service, Controller 분리)
- Dependency Injection 기본 제공
- 데코레이터 기반 선언적 API
- 강력한 CLI 도구
- Angular와 유사한 아키텍처 (학습 곡선 있지만 유지보수성 높음)

**웹 검색 결과** (2025-10-26):
- NestJS + Express: 17K RPS @ 200 concurrent connections
- NestJS + Fastify: 50K RPS @ 200 concurrent connections (3배 빠름)
- Fastify는 Express 대비 82% 적은 CPU 사용, 167% 많은 메모리 사용
- NestJS는 Express ↔ Fastify 전환이 쉬움

**대안 검토**:
- **Express (Raw)**: 가볍고 간단
  - 장점: 생태계 방대, 학습 용이, 튜토리얼 많음
  - 단점: 구조화 부족, 대규모 프로젝트에서 유지보수 어려움
  - **평가**: Phase 1+2에서는 구조화가 중요하므로 제외

- **Fastify (Raw)**: 고성능
  - 장점: Express보다 3배 빠름, TypeScript 기본 지원, 플러그인 아키텍처
  - 단점: 생태계 작음, 학습 곡선, DDD 구조화 어려움
  - **평가**: NestJS + Fastify 조합으로 나중에 고려 가능

- **Hapi.js**: 엔터프라이즈급
  - 장점: 보안 강화, 플러그인 시스템
  - 단점: 생태계 작음, 러닝 커브
  - **평가**: 오버스펙, 제외

**최종 선정**: NestJS (기본 Express, Phase 3에서 Fastify 전환 고려)

---

### 프론트엔드

#### UI 라이브러리: React

**선택**: React

**버전**: `^18.3.0` (최신 안정 버전)

**선정 이유**:
- 가장 큰 생태계 (npm 다운로드 Vue의 3배)
- 컴포넌트 재사용성 높음
- Hooks 기반 상태 관리 직관적
- 풍부한 라이브러리 (Monaco Editor, CodeMirror 등)
- 채용 시장에서 수요 높음
- React Native로 향후 모바일 앱 확장 가능

**웹 검색 결과** (2025-10-26):
- React: 7천만+ 웹사이트, 216k GitHub stars
- Vue: 800만+ 웹사이트, 40.6k GitHub stars
- React의 npm 다운로드 Vue의 3배
- React는 대규모 애플리케이션에 최적화
- Vue는 초기 학습 곡선이 낮고 번들 크기 작음

**대안 검토**:
- **Vue 3**: 간결하고 학습 용이
  - 장점: 빠른 학습, 작은 번들 크기, 빠른 초기 로드
  - 단점: 생태계 작음, 채용 시장 작음, 모바일 지원 약함
  - **평가**: Phase 1+2의 단순한 에디터에는 적합하지만, 향후 확장성 고려 시 React 선택

- **Svelte**: 최신 프레임워크
  - 장점: 컴파일 타임 최적화, 작은 번들, 빠른 성능
  - 단점: 생태계 매우 작음, 레퍼런스 부족, 채용 어려움
  - **평가**: 프로덕션 사용엔 리스크 높음, 제외

**최종 선정**: React

---

#### 빌드 도구: Vite

**선택**: Vite

**버전**: `^5.x`

**선정 이유**:
- 매우 빠른 개발 서버 (Hot Module Replacement)
- Rollup 기반 프로덕션 빌드
- TypeScript 네이티브 지원
- React 공식 지원
- Create React App보다 10배 빠름

**대안 검토**:
- **Webpack**: 전통적 번들러
  - 장점: 성숙한 생태계, 플러그인 많음
  - 단점: 복잡한 설정, 느린 빌드
  - **평가**: Vite가 모든 면에서 우수, 제외

- **Turbopack (Next.js)**: 차세대 번들러
  - 장점: Rust 기반 초고속
  - 단점: Next.js 전용, 단독 사용 불가
  - **평가**: Next.js 사용 안 하므로 제외

**최종 선정**: Vite

---

### 데이터베이스 (선택적)

#### RDBMS: PostgreSQL

**선택**: PostgreSQL (Phase 3 고려)

**버전**: `^16.x` (최신 안정 버전)

**현재 단계**: 사용하지 않음 (인메모리 저장)

**향후 사용 이유**:
- 오픈소스, 무료
- JSON 타입 지원 (유연한 스키마)
- 강력한 인덱싱 및 쿼리 성능
- TypeORM과 완벽 통합
- Docker로 쉬운 배포

**대안 검토**:
- **MySQL**: 대중적
  - 장점: 대중적, 많은 호스팅 지원
  - 단점: JSON 지원 약함, 고급 기능 부족
  - **평가**: PostgreSQL이 더 나은 선택

- **MongoDB**: NoSQL
  - 장점: 스키마리스, JSON 네이티브
  - 단점: 트랜잭션 복잡, DDD Aggregate 매핑 어려움
  - **평가**: RDBMS가 DDD에 더 적합, 제외

- **SQLite**: 경량
  - 장점: 파일 기반, 설치 불필요
  - 단점: 동시 쓰기 제한, 프로덕션 부적합
  - **평가**: 개발용으로만 사용 가능, 제외

**최종 선정**: PostgreSQL (Phase 3)

---

## 핵심 라이브러리

### Mermaid.js

**선택**: Mermaid.js

**버전**: `^11.12.0` (최신 버전, 2025-10 기준)

**선정 이유**:
- Mermaid 렌더링의 공식 라이브러리
- 모든 다이어그램 타입 지원 (flowchart, sequence, class, ER, gantt 등)
- 활발한 업데이트 (최근 1개월 전)
- 오픈소스, 무료

**웹 검색 결과** (2025-10-26):
- 최신 버전: v11.12.0
- 최근 업데이트: 새로운 participant 타입 추가 (actor, boundary, control, entity, database 등)
- 개선된 mindmap 렌더링, 여러 레이아웃 지원
- Tiny 버전 제공 (약 50% 사이즈, mindmap/architecture 제외)

**대안**:
- 없음 (Mermaid 전용 프로젝트)

**최종 선정**: Mermaid.js v11.12.0

---

## 개발 도구

### 패키지 매니저

**선택**: pnpm

**버전**: `^9.x`

**선정 이유**:
- npm/yarn보다 빠름
- 디스크 공간 절약 (심볼릭 링크)
- 엄격한 의존성 관리
- Monorepo 지원

**대안**: npm, yarn
**최종 선정**: pnpm

---

### Linter/Formatter

**선택**: ESLint + Prettier

**버전**: ESLint `^9.x`, Prettier `^3.x`

**선정 이유**:
- 코드 품질 및 일관성 유지
- TypeScript 완벽 지원
- 업계 표준

---

### 컨테이너

**선택**: Docker + Docker Compose

**버전**: Docker `^27.x`, Docker Compose `^2.x`

**선정 이유**:
- 로컬 개발 환경 통일
- 배포 간소화
- 프로덕션과 동일한 환경

---

## 승인 상태 (1부)

**전체 승인 상태**: ✅ (승인 완료)

**승인 체크리스트**:
- [x] 백엔드 언어 및 프레임워크 승인 (TypeScript + NestJS)
- [x] 프론트엔드 라이브러리 승인 (React + Vite)
- [x] 데이터베이스 선택 승인 (PostgreSQL, Phase 3)
- [x] Mermaid.js 버전 승인

**승인 날짜**: 2025-10-26

---

## 2️⃣ 기능별 라이브러리 선정

### 백엔드 - Validation

#### class-validator

**선택**: class-validator + class-transformer

**버전**: class-validator `^0.14.x`, class-transformer `^0.5.x`

**선정 이유**:
- NestJS 공식 권장 라이브러리
- Decorator 기반으로 DDD Entity/DTO와 완벽 매칭
- TypeScript 타입과 validation 규칙이 한 곳에
- NestJS ValidationPipe와 기본 통합
- Class 기반 아키텍처와 자연스러운 통합

**웹 검색 결과** (2025-10-26):
- class-validator는 TypeScript 및 NestJS 프로젝트에 이상적
- Decorator를 사용하여 클래스 속성에 validation 규칙 정의
- Class 기반 구조에서 가독성과 유지보수성 우수
- Zod는 최근 급성장, TypeScript-first, 단일 소스 진실 (schema = type)
- Yup은 React + Formik에서 인기, 체이닝 API

**대안 검토**:
- **Zod**: TypeScript-first, 제로 의존성
  - 버전: `^3.x`
  - 장점: 스키마가 곧 타입, 가장 강력한 타입 추론, 제로 의존성
  - 단점: NestJS와 통합 시 추가 설정 필요, class-validator만큼 decorator 친화적이지 않음
  - 웹 검색: 2024-2025년 가장 빠르게 성장, TypeScript 프로젝트에 최적
  - **평가**: Phase 3에서 프론트엔드 form validation용으로 고려 가능

- **Yup**: 스키마 빌더
  - 버전: `^1.x`
  - 장점: React + Formik과 좋은 통합, 간단한 API, 오랜 검증
  - 단점: TypeScript 타입 추론 약함, 백엔드보다 프론트엔드 친화적
  - 웹 검색: React 생태계에서 여전히 인기
  - **평가**: 프론트엔드용으로만 고려

**최종 선정**: class-validator (백엔드), Zod (프론트엔드 form, Phase 3 고려)

---

### 백엔드 - Image Export

#### Puppeteer

**선택**: Puppeteer

**버전**: `^23.x`

**선정 이유**:
- SVG → PNG 변환에 가장 신뢰성 높음
- Headless Chrome 기반으로 복잡한 SVG 렌더링 정확
- 널리 사용되고 검증됨 (87k GitHub stars)
- 단순한 API, 풍부한 문서

**웹 검색 결과** (2025-10-26):
- Puppeteer: 87k GitHub stars, Chromium 전용
- Playwright: 64k GitHub stars, 3개 브라우저 지원 (Chromium, Firefox, WebKit), Microsoft 개발
- Playwright가 크로스 브라우저 지원 및 현대적 기능으로 2025년 권장
- SVG → PNG: Puppeteer/Playwright는 렌더링 에러 없음, node-canvas는 경량이지만 복잡한 SVG에서 문제 발생 가능

**대안 검토**:
- **Playwright**: Microsoft의 크로스 브라우저 자동화
  - 버전: `^1.x`
  - 장점: 3개 브라우저 지원, auto-wait 기능, 더 빠름, Microsoft 지원
  - 단점: Puppeteer보다 무거움, 프로젝트에서 크로스 브라우저 불필요
  - 웹 검색: 2025년 신규 프로젝트 권장, 더 모던한 API
  - **평가**: Phase 3에서 고려 가능 (현재는 Chromium만 있으면 충분)

- **node-canvas**: 경량 Canvas 구현
  - 버전: `^2.x`
  - 장점: 매우 가벼움, 네이티브 모듈, 빠른 변환
  - 단점: 복잡한 SVG 렌더링에서 에러 가능, CSS 지원 제한
  - 웹 검색: 간단한 SVG에는 적합
  - **평가**: Mermaid SVG는 복잡할 수 있어 제외

**최종 선정**: Puppeteer (Phase 3에서 Playwright 고려)

---

### 프론트엔드 - 상태 관리

#### Zustand

**선택**: Zustand

**버전**: `^5.x`

**선정 이유**:
- 매우 간단한 API, 최소 보일러플레이트
- 작은 번들 크기 (~3KB)
- Context Provider 불필요
- Hooks 기반, React와 자연스러운 통합
- 90% SaaS/MVP에서 권장

**웹 검색 결과** (2025-10-26):
- Zustand: 다재다능한 중간 지점, 대부분 프로젝트에 적합
- Redux Toolkit: 엔터프라이즈급, 큰 팀에 적합, 구조화된 패턴
- Jotai: Atomic 모델, 세밀한 리렌더링 제어, 복잡한 상태 의존성에 최적
- 2025 권장: Context → Zustand → Jotai (복잡도 순)

**대안 검토**:
- **Redux Toolkit**: 엔터프라이즈 표준
  - 버전: `^2.x`
  - 장점: 구조화된 패턴, 큰 팀에 적합, 예측 가능, DevTools 강력
  - 단점: 보일러플레이트 많음, 러닝 커브, 작은 프로젝트에는 오버스펙
  - 웹 검색: 대규모 팀 및 복잡한 앱에 여전히 강력
  - **평가**: 현재 프로젝트 범위에 오버스펙, 제외

- **Jotai**: Atomic 상태 관리
  - 버전: `^2.x`
  - 장점: 세밀한 리렌더링 제어, 가장 높은 성능, atom 기반 모델
  - 단점: 새로운 멘탈 모델, 러닝 커브, 작은 프로젝트에는 복잡
  - 웹 검색: 복잡한 상태 의존성에 최적, 성능 최고
  - **평가**: 현재 프로젝트는 상태가 단순하여 불필요, 제외

- **Context API**: React 내장
  - 장점: 추가 라이브러리 불필요
  - 단점: 리렌더링 이슈, 복잡한 상태 관리 어려움
  - **평가**: 간단한 상태만 있다면 가능하지만, Zustand가 더 나음

**최종 선정**: Zustand

---

### 프론트엔드 - Code Editor

#### Monaco Editor

**선택**: Monaco Editor (@monaco-editor/react)

**버전**: `^4.x`

**선정 이유**:
- VS Code의 에디터 엔진 (업계 표준)
- 강력한 IntelliSense, 자동완성, 문법 하이라이팅
- Mermaid 문법 지원 가능 (커스텀 언어 정의)
- 큰 파일 처리 성능 우수 (가상화 렌더링)
- React 통합 라이브러리 제공

**웹 검색 결과** (2025-10-26):
- Monaco Editor: 51MB 번들 (5MB gzipped), VS Code 기반, IntelliSense 지원, 큰 파일 최적화
- CodeMirror 6: 8MB 번들 (1.26MB gzipped), 더 가벼움, 모바일 지원 우수, 커스터마이징 쉬움
- Monaco는 본격적인 코드 편집에 적합, CodeMirror는 짧은 스니펫에 적합
- CodeMirror 6는 완전한 재작성으로 접근성, 터치스크린 지원 향상

**대안 검토**:
- **CodeMirror 6**: 경량 에디터
  - 버전: `^6.x`
  - 장점: 작은 번들 (Monaco의 1/6), 빠른 성능, 모바일 지원 우수, 커스터마이징 쉬움
  - 단점: IntelliSense 없음, 기능 적음, Mermaid 문법 확장 추가 작업
  - 웹 검색: Replit이 Monaco에서 CodeMirror로 전환 (성능 + 커스터마이징)
  - **평가**: Phase 1+2에서는 Monaco의 강력한 기능이 더 유리, Phase 3에서 경량화 필요 시 고려

- **Ace Editor**: 레거시
  - 장점: 오래된 검증, 많은 언어 지원
  - 단점: 무거움, 모던하지 않음, 유지보수 저조
  - **평가**: 제외

**최종 선정**: Monaco Editor (Phase 3에서 CodeMirror 6 고려)

---

### 프론트엔드 - HTTP Client

#### Axios

**선택**: Axios

**버전**: `^1.x`

**선정 이유**:
- 요청/응답 인터셉터 (에러 처리, 인증 토큰 등)
- 자동 JSON 변환
- 타임아웃 설정 간편
- 브라우저 및 Node.js 양쪽 지원
- 취소 요청 지원 (CancelToken)

**대안 검토**:
- **Fetch API**: 브라우저 내장
  - 장점: 추가 라이브러리 불필요, 가벼움, 모던 Promise 기반
  - 단점: 인터셉터 없음, 타임아웃 설정 복잡, JSON 변환 수동
  - **평가**: 간단한 프로젝트면 Fetch로 충분하지만, Axios의 편의 기능이 유용

- **ky**: 모던 Fetch 래퍼
  - 버전: `^1.x`
  - 장점: Fetch 기반, 가벼움, 타임아웃/재시도 기본 제공
  - 단점: 생태계 작음, Axios만큼 기능 많지 않음
  - **평가**: Axios가 더 검증되고 안정적

**최종 선정**: Axios

---

### 프론트엔드 - UI 컴포넌트 (선택적)

#### Tailwind CSS

**선택**: Tailwind CSS

**버전**: `^3.x`

**선정 이유**:
- Utility-first CSS, 빠른 개발
- 커스터마이징 쉬움
- 작은 프로덕션 번들 (unused 제거)
- 디자인 시스템 없어도 일관성 유지

**대안 검토**:
- **Material UI (MUI)**: React 컴포넌트 라이브러리
  - 장점: 완성도 높은 컴포넌트, 빠른 프로토타이핑
  - 단점: 무거움, 커스터마이징 어려움, 디자인 제약
  - **평가**: Phase 1+2의 간단한 UI에는 오버스펙

- **Chakra UI**: 모던 컴포넌트 라이브러리
  - 장점: 접근성 우수, 간단한 API
  - 단점: MUI보다 작지만 여전히 무거움
  - **평가**: Tailwind로 충분

**최종 선정**: Tailwind CSS

---

## 승인 상태 (2부)

**전체 승인 상태**: ✅ (승인 완료)

**승인 체크리스트**:
- [x] Validation 라이브러리 승인 (class-validator, Zod)
- [x] Image Export 라이브러리 승인 (Puppeteer)
- [x] 상태 관리 라이브러리 승인 (Zustand)
- [x] Code Editor 라이브러리 승인 (Monaco Editor)
- [x] HTTP Client 승인 (Axios)
- [x] UI 라이브러리 승인 (Tailwind CSS)

**승인 날짜**: 2025-10-26

---

## 전체 승인 상태

**1부 (언어 및 프레임워크)**: ✅ 승인 완료

**2부 (기능별 라이브러리)**: ✅ 승인 완료

---

> 📎 **관련 문서**:
> - `documents/architecture/system-overview.md`
> - `documents/domain-modeling/bounded-contexts.md`
> - `documents/api/rendering-api.md`
