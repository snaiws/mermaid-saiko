# Node Environment
NODE_ENV=development

# Database Configuration (mlops pgvector 인프라 사용)
# mlops pgvector DB에 연결 - /workspace/Projects/mlops/mlops/pgvector 참고
DB_HOST=localhost
DB_PORT=50018
DB_USER=admin
DB_PASSWORD=1234
DB_NAME=mermaid_saiko

# Server Configuration
PORT=3000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Mermaid Configuration
MERMAID_TIMEOUT=5000

# Puppeteer Configuration (for PNG export)
PUPPETEER_HEADLESS=true
